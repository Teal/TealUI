## TealUI 4.0 更新计划

重构所有组件：
- 所有 CSS 使用 SASS 重写。
- 所有 JS 使用 ES6(JSX) 重写。
- 所有示例 HTML 使用 Markdown 重写。
- 使用 tpack 进行打包。

增强组件支持：
- 解耦 UI 组件，针对 PC、H5、Hybrid、React Native 进行 UI 组件分离。
- 新组件不再支持 IE9 及以下版本兼容。老组件被移动到 legency 分类并保持 IE6 兼容。

